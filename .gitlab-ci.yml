before_script:
  - cd terraform
  - mkdir creds
  - echo $SERVICEACCOUNT

stages:
  - terraform

terraform:
  tags:
    - docker
  stage: terraform
  script:
    - ./terraform init
    - ./terraform apply